data Maybe where
| Just Int
| Nothing

def fromMaybe (fallback : Int) (m : Maybe) -> Int do
  match m with
  | Just x -> x
  | Nothing -> fallback
  end

def main () -> Int do
  fromMaybe 0 (Just 42)
